const initialState = {
  answers: [],
  loading: true,
  error: null,
};


export const answersReducer = (state = initialState, action ) : AnswersState => {
    switch(action.type) {
        
    }
}



